apiVersion: route.openshift.io/v1
kind: Route
metadata:
  name: qwen25-7b-instruct
  namespace: my-first-model
  labels:
    app: qwen25-7b-instruct
  annotations:
    description: "Route to expose vLLM OpenAI-compatible API"
spec:
  to:
    kind: Service
    name: qwen25-7b-instruct-api
    weight: 100
  port:
    targetPort: 8080
  tls:
    termination: edge
    insecureEdgeTerminationPolicy: Redirect
  wildcardPolicy: None
